<% content_for :title, "Editing user" %>

<nav class="navbar d-flex space-between align-items-center">
	<h1 class="d-block text-truncate w-50 mb-0">Editing user</h1>

	<div class="d-flex gap-2 gap-lg-3 align-items-center">
		<%= link_to @user, class: "btn btn-outline-secondary", title: "Cancel" do %>
			<i class="bi bi-x"></i>
			<span class="d-none d-md-inline-block">Cancel</span>
		<% end %>

		<%= link_to root_url, class: "btn btn-outline-secondary", title: "Back" do %>
			<i class="bi bi-arrow-left"></i>
			<span class="d-none d-md-inline-block">Back</span>
		<% end %>

		<%= button_tag type: :submit, name: :commit, form: :edit_user, class: "btn btn-outline-success" do %>
			<i class="bi bi-plus"></i> Save
		<% end  %>
	</div>
</nav>

<%= render "form", user: @user %>
